declare class MarkerClusterer {
    constructor(map: any, opt_markers: any[], opt_opts: any[]);
    public addMarkers(markers: any, opt_nodraw: any);
    public addMarker(marker: any, opt_nodraw: any);
    public clearMarkers();
}
